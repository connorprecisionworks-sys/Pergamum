// Templates were removed from the prompt builder in favour of a cleaner
// chat-first empty state. This file is intentionally empty and can be
// deleted once the next commit lands. Kept around only because git already
// has the original file staged elsewhere; the easiest path to a clean
// commit is to overwrite it here, then `git rm lib/prompt-templates.ts`
// from a real terminal where this directory is writable.
export {};
